// Responsive Navbar (optional dropdown for mobile)
document.addEventListener("DOMContentLoaded", () => {
  const navLinks = document.querySelector('.nav-links');

  // If you want a burger menu, add it in HTML and add toggle logic here
});

